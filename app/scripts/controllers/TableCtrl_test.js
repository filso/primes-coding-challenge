describe('TableCtrl', function() {
  'use strict';
  var scope, $httpBackend;

  beforeEach(module('app'));

  beforeEach(inject(function($controller) {
    scope = {};
    $controller('TableCtrl', scope);
  }));

  it('should generate table data', function() {

  });

  // it('should ')

});